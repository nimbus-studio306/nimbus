# 2026-02-07 — System Fixes & Stability

## DB Watcher Stability Fix
- **Problem**: DB watcher process kept dying after OpenClaw exec session cleanup
- **Root cause**: `nohup` wasn't enough — process still attached to exec session
- **Solution**: Changed `startup.sh` to use `setsid` for proper process detachment
- **Command**: `setsid python3 db-watcher.py > db-watcher.log 2>&1 &`

## Extraction Queue Fixes
- Fixed JSON parse errors with multi-strategy repair logic in `graph-extract.py`
- Fixed type errors: Added `isinstance(entity, dict)` and `isinstance(rel, dict)` checks
- Fixed 'str' object error in category linking (line 403)
- Reset 79 queue errors + 2 stuck processing items for retry
- Queue status at end of session: 1385 done, 964 pending, 0 errors

## Browser Automation Fix
- **Problem**: Playwright couldn't launch browser in container
- **Solution**: `attachOnly: true` mode — manually start chromium, gateway attaches
- **Config**: `/home/node/.openclaw/openclaw.json` → `browser.executablePath`, `attachOnly`
- **Port**: 18800 for CDP
- **Startup command**: `setsid /usr/bin/chromium --headless --no-sandbox --disable-gpu --remote-debugging-port=18800 --user-data-dir=/home/node/.openclaw/browser/openclaw/user-data about:blank`
- Added chromium auto-start to startup.sh

## Short Node Embedding Fix
- Embedded 3 short-named nodes (AI, DB, jq) with expanded context text
- Fixed issue where short names couldn't generate meaningful embeddings

## Commits
- f2cada6, 31ed5de, 4d508b1, 48cd055, 9c3d004, c312765

## Knowledge Graph Stats
- 251 nodes, 507 edges (at session end)

## Pending
- Funkin'4 Holland screenshot: Cookie banner needs dismissing for clean capture
- Queue still processing (964 pending)
- Consider adding watchdog/restart logic inside db-watcher.py itself
