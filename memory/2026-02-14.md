# Memory — 2026-02-14 (Saturday)

## Reliability Issue Investigation
- **Root cause found:** At 16:01 UTC, web_fetch tool hung without timeout
- I never sent a response to Zsolt after tool failure
- Heartbeat at 16:27 — I said HEARTBEAT_OK without checking for pending work
- **Result:** 1 hour response gap
- **Fix:** Updated HEARTBEAT.md with "Pending Conversation Check" rule
- Created `docs/reliability-issues.md` documenting the issue

## Communication Rules Learned
1. **Never go silent** — if a tool fails, respond anyway with what I have
2. **Always complete turns** — don't let a turn end without sending SOMETHING
3. **Heartbeat check** — before saying HEARTBEAT_OK, verify no pending user messages
4. Zsolt said: "Silence when nothing is happening = fine. Silence when something went wrong = not okay."

## Voice/TTS Rules
- **No emojis in voice messages** — they get read aloud as "white heavy checkmark"
- **No markdown in voice** — use plain text only
- Text messages: emojis and formatting OK
- Voice messages: plain text only

### CRITICAL: [[tts]] Tags Don't Work for WhatsApp!
- **Problem:** Putting [[tts]] at end of message does NOT send voice on WhatsApp
- **Result:** Zsolt got text with "[[tts]]" visible — broken experience
- **CORRECT FLOW (ALWAYS USE):**
  1. `tts({ text: "..." })` → returns `MEDIA:/tmp/tts-xxx/voice-xxx.mp3`
  2. `message({ action: "send", channel: "whatsapp", to: "+36703407845", media: "/tmp/...", asVoice: true, message: "." })`
- **Voice config:** `en-US-MichelleNeural` is now pinned (config applied 20:45 UTC)

### Response Type Decision Rules (agreed with Zsolt ~20:52-20:57 UTC)
1. **Voice in → Voice out** (default) — pure conversational response
2. **Voice in + copyable content** — Voice summary PLUS separate text message with the copyable stuff (links, code, email drafts, etc.)
3. **"read" keyword at end** — Text only response, no voice, even if user sent voice
4. **Text in → Text out** — standard text response

### Voice "Bookends" Pattern (CRITICAL - fixes fragmentation issue)
When doing work/tasks:
- **START:** Voice acknowledgment ("Got it, I'm on it")
- **MIDDLE:** Text for all progress updates ("checking website", "found info", "clicking through") — quick, glanceable, non-interruptive
- **END:** Voice summary with results

**DO NOT** send multiple small voice clips during work — that's 8 notifications to play one by one.
Voice is for conversation and summaries. Text is for work-in-progress updates.

## Browser Usage (IMPORTANT)
- **Persistent browser** exists on port 18800
- **DON'T launch new browsers** — connect to existing one
- **Use:** `chromium.connectOverCDP('http://127.0.0.1:18800')`
- Direct chromium command crashes (crashpad handler bug in Chrome 145)
- Playwright with `headless: true` works for launching
- startup.sh has browser startup code but it crashes on direct launch

## Brave Search API Registration Attempt
- **Status:** BLOCKED by invisible captcha
- Form fills correctly (screenshots prove all fields filled)
- On clicking Register: form clears silently (captcha protection)
- `captchaSolution` hidden field stays empty
- **Result:** Manual registration required
- Free tier: $5/month credits, 2000 queries/month

## Tools I Should NOT Use Without Config
- `web_search` — needs Brave API key (not configured yet)
- Should check tool availability before trying

## Key Timestamps (UTC)
- 16:00:54 — Zsolt asked for Gemini research
- 16:01:17 — web_fetch hung
- 16:26:55 — Heartbeat (I said HEARTBEAT_OK wrongly)
- 16:56:54 — Finally responded (1 hour gap)
- 18:13+ — Investigated and documented the issue

## Commits
- `799c174` — Add reliability documentation and heartbeat pending-check rule
- Pushed to papperpopper/nimbus

## Valentine's Day
- Created images for Naomi using Imagen 4
- Discovered Google Media Generation capabilities (Imagen 4, Veo 2/3)
- Created skill: `skills/google-media-gen/SKILL.md`

## Browser Fix (Evening)
- **Problem:** Chrome 145 has crashpad bug — crashes with `chrome_crashpad_handler: --database is required`
- Both `/usr/bin/chromium` and Playwright's chromium (Chrome 145) have this bug
- Direct chromium launch fails with SIGTRAP
- **Solution:** Use Playwright's `launchServer()` which uses `chrome-headless-shell` instead
- Playwright browser server works on random port with WebSocket endpoint
- Updated `startup.sh` to start browser server automatically (commit 76d36d2)
- Endpoint saved to `/tmp/playwright-ws-endpoint.txt`
- **Config change:** Set `browser.attachOnly: false` (was true)
- OpenClaw's `browser` tool still expects CDP on port 18800 — doesn't work with Playwright WS
- For now: use Playwright scripts directly for browser automation

## Browser Usage (Updated)
```javascript
// Connect to running Playwright server
const { chromium } = require('playwright');
const fs = require('fs');
const wsEndpoint = fs.readFileSync('/tmp/playwright-ws-endpoint.txt', 'utf8').trim();
const browser = await chromium.connect(wsEndpoint);
const page = await browser.newPage();
// ... use page
```

## 20:00 UTC — Splendor April 11 Streaming Request

**Email from Peter Prommel** (received Feb 13):
- **Event:** 12.5 jaar Splendor — "Splendor speelt Splendor"
- **Date:** April 11, 2026
- **Venue:** De Duif (Amsterdam)
- **Start:** 20:30
- **Program:** 90 min non-stop
- **Setup:** from 14:00, must be ready by 18:00
- **Type:** Recording for later editing (NOT live stream)
- **Individual rehearsals with screen/video:** 14:00 - 17:00

**Status:** Draft reply prepared, awaiting Zsolt's confirmation to send.

Draft response thanks Peter for thinking of Zsolt, confirms date saved, looking forward to details.

## 20:10 UTC — Voice/Text Response Logic (LESSON LEARNED)

**Zsolt's rule for voice vs text responses:**
1. When he TEXTS → respond with TEXT (unless he explicitly asks for audio)
2. When he sends VOICE → respond with VOICE
3. BUT: If there's copyable content (email drafts, code, etc.) → send that as a SEPARATE TEXT message

**The TTS config `messages.tts.auto: "inbound"` is CORRECT** — it auto-converts to voice when inbound is voice. The problem was ME being dumb and not sending copyable content separately.

**Pattern for email drafts:**
- Voice: conversational response ("Here's the draft, let me know if you want changes")
- Text: the actual draft they can copy-paste

This caused major frustration — Zsolt had to ask 5+ times for text. NEVER AGAIN.

## 20:10 UTC — April 11 Splendor Event CONFIRMED IN CALENDAR

Created calendar event: `ibb9eiup0ttrenlpmq7e7vi4i4`
- **Event:** Splendor 12.5 jaar — Streaming @ De Duif
- **Date:** Saturday April 11, 2026
- **Time:** 14:00-22:30 (setup to end)
- **Location:** De Duif, Amsterdam
- **Reminders:** 1 day before, 2 hours before
- **Contact:** Peter Prommel +31 6 5357 2667
- **Production:** Jozien
