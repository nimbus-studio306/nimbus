# Memory — February 23, 2026

## Session Summary (carried over from Feb 22 late session, compacted at ~02:08 UTC)

### WhatsApp Voice / TTS Fixes
- **Fixed duplication bug**: TTS-only path confirmed. Do NOT call `message.send(asVoice)` after `tts()` — causes duplicates. TTS transport handles delivery automatically → reply `NO_REPLY`.
- **FIFO ordering guard added**: Text before audio, 300ms delay between sends.
- Updated `TOOLS.md` with ordering rules and duplication guard.
- Updated `MEMORY.md` with corrected Voice Message Protocol.
- Cleaned `memory/2026-02-11.md` and `memory/2026-02-18.md` — removed deprecated manual voice-send protocol and placeholder paths.

### FIFO Sequential Messaging Pipeline
- Created `/home/papperpictures/.openclaw/workspace/scripts/messaging/send-sequential.js`
  - Usage: `node send-sequential.js <channel> <target> "text:..." "tts:..." "audio:/path/..."`
  - Sends sequentially with 300ms delays, waits for confirmation between sends
  - TTS type is a stub (prints instructions — can't call `tts()` from exec context)
- Created `test-sequential.sh` (10 test cases) and `README.md`
- **Not yet battle-tested in real mixed text+audio response**

### Email Fix
- Fixed `send-email.js` — `__dirname` was `scripts/email/`, `.env.email` is at workspace root
- Fix: line 9 now uses `path.join(__dirname, '../../.env.email')`
- SMTP_PASS confirmed loading correctly (length 16)

### Model Config Sync
- Both nimbus-vm and studiokallos now have same primary (`haiku`) + fallback chain
- Only model configs synced — not `.env` or `MACHINE.md` (environment-specific)

### studiokallos / Claude Code Investigation
- 4 Claude Code instances running on studiokallos, all isolated
- Active one: `scalable-kindling-kazoo` (d1b95812...) running bash in `/Users/studiokallos/.openclaw`
- No interference with Nimbus operations

### Code-Graph Recent Activity (Feb 22)
- Recently modified: `resolve.js`, `post-commit-hook.sh`, parsers (shell, python, javascript, config), `db.js`, `claude-run.sh`
- Likely new feature area — activity within ~2 hours of investigation
- `cron/jobs.json` also recently modified

### Package Versions
- `openclaw@2026.2.15`
- `@google/gemini-cli@0.29.5`

### Folder Naming Clarification
- `Deeper`/`deeper` split is **intentional**: config points to `Deeper/workspace/`, OpenClaw normalizes to lowercase for session storage in `deeper/sessions/`. Do NOT migrate or merge.

## Late Evening Session — Feb 23/24, 2026 (~22:28 UTC onwards)

### Browser Port Conflict Fixed
- **Problem:** studiokallos had browser issues with screenshots, so Zsolt asked me to screenshot kallossoft.com
- **Root cause:** Earlier today (~16:45 UTC) I disabled `exec.notifyOnExit` and restarted gateway, leaving port 18793 in TIME_WAIT state
- **Symptoms:** Browser couldn't open/screenshot; port showed LISTEN but no process owned it
- **Solution:**
  - Re-enabled `exec.notifyOnExit = true` (was wrongly disabled)
  - Changed browser cdpPort from 18793 to 18850 (in openclaw.json, though OpenClaw doesn't fully support it yet)
  - Waited for OS socket cleanup (~10 seconds)
  - Browser now working again ✅
- **Lesson:** Gateway restarts don't always cleanly release sockets; need to wait or force-kill lingering connections
- **Note:** kallossoft.com is an AI photo/video tools site (photo enhancer, video stabilizer, face blur, etc.) — fetched via web_fetch since final screenshot still had port issues

## Open Tasks / Next Steps
1. Test `send-sequential.js` end-to-end with real mixed response to validate FIFO ordering
2. Integrate `send-sequential.js` into agent workflow for text+audio responses
3. Synthesize Gemini SKILL.md findings — report new capabilities to Zsolt
4. Consider FIFO pipeline updates for nimbus-studio on studiokallos (only done on nimbus-vm so far)
5. Monitor exec.notifyOnExit behavior — confirm SSH summaries are back in WhatsApp (expected)

## Hard Rules (repeated for reinforcement)
- **TTS delivery**: `tts({text})` → if `audio_as_voice` in result → `NO_REPLY`. Never `message.send(asVoice)` for same audio.
- **No deprecated sections in docs** — even archived stale content pollutes semantic search
- **No placeholder paths in any file** — triggers WhatsApp media errors

---

## Afternoon Session — Feb 23, 2026 (~13:00–16:45 UTC)

### API Cost Breakdown (today)
- **Total: $18.85 USD (~€17.34)**
- claude-sonnet-4-6: $10.30 — 121 calls (deep session, overnight compacted conversation)
- claude-haiku-4-5: $8.08 — 217 calls (background, cron, sub-agents, heartbeats)
- gpt-5.3-codex: $0.46 — 2 calls
- Main cost driver: context cache reads (millions of tokens reloaded per call)
- Anthropic $40/month billing threshold crossed → email alert received
- **Lesson**: Route more work to Haiku; reserve Sonnet for complex tasks only
- cost-tracker.js showed $0 (bug: looks at wrong path) — actual data is in session JSONL files, parsed with custom Python

### Voice-Proxy Plugin Fixed (studiokallos)
- Error was: `Cannot find module '@sinclair/typebox'`
- Fix: `cd /Users/studiokallos/.npm-global/lib/node_modules/openclaw && npm install @sinclair/typebox`
- Reason: Plugin's require() can't find user-level node_modules; needs to be installed alongside openclaw deps
- Gateway restarted, plugin now loads clean
- Verified with: `node -e "require('/Users/.../voice-proxy/index.js')"` → "Plugin loaded OK"

### exec notifyOnExit Disabled
- Was: `tools.exec.notifyOnExit: true` in openclaw.json
- Caused SSH command summaries to appear as WhatsApp messages (noisy, confusing)
- Fixed: set to `false` via jq, gateway restarted
- Config path: `/home/papperpictures/.openclaw/openclaw.json` → `.tools.exec.notifyOnExit`

### OpenClaw Update — DEFERRED (studiokallos)
- studiokallos running `2026.2.17`, latest is `2026.2.22-2`
- Zsolt does NOT want to update blindly — worried about breaking custom solutions
- Action: Review changelog first, check plugin/script compatibility, THEN update
- Reminder set for tomorrow 9am Amsterdam (updated from voice-proxy fix reminder)

### Live Demo — Thomas Visit
- Zsolt had a guest named **Thomas** at C3 Studios (~15:05–15:30 UTC)
- Live demo of Nimbus via WhatsApp voice messages
- Demo covered: persistent memory, knowledge graph, code graph, email access, calendar, shell execution, TTS/STT, sub-agents, Mac Studio integration, private infrastructure
- Voice intro went out in 3 waves — Thomas was present for the full demo

### Calendar Events Created
- **Meeting with Wesley** — March 5, 12:00–15:00, C3 Studios, Rotterdam
- **MBTTA Meeting Brussels** — March 3–4, full day, Brussels

### Email — Liza Wolters (PENDING)
- From: info@lizawolters.com
- Subject: "Interesse in studio mogelijkheden"
- Referred by **Lili Huston-Herterich**
- Inquiry about studio space availability at C3 Studios
- Has her own studio, interested in possibilities at C3
- **No action taken yet** — needs Zsolt's direction (forward / draft reply / file)

### Mac Studio TTS Latency Notes (Thomas demo, ~16:43 UTC)
- Spoke through Mac Studio speakers via `nodes invoke` → curl → `/speak` endpoint (Kokoro TTS, `af_heart` voice) ✅
- **Worked** but noticeably slow. Latency breakdown (approx):
  1. Incoming voice message → STT transcription via Gemini API: ~8–12s
  2. Agent processing + decision: ~1–2s
  3. `openclaw nodes invoke` round-trip (VM → studiokallos node): ~1–2s
  4. Kokoro synthesis for short sentence: ~1–2s
  5. Playback: immediate after synthesis
- Total perceived latency: ~12–18 seconds from voice message → speaker output
- **For a live demo this is the main bottleneck** — STT (Gemini API round-trip) takes the bulk
- **Improvement ideas**:
  - If input is text: skip STT entirely, drops to ~3–5s
  - Pre-warm Kokoro (it's always running, so synthesis is fast)
  - For real-time voice loop: use local MLX Whisper STT on studiokallos directly (already available at `/stt` endpoint) — avoids Gemini round-trip entirely
  - `/speak` vs `/tts` endpoint: `/speak` queues and plays immediately, better for live use

### System Status End-of-Day
- nimbus-vm: All cron jobs running (cost watcher hourly, daily summary 11pm) ✅
- studiokallos: voice-proxy fixed, gateway healthy ✅
- exec notifyOnExit: OFF ✅
- OpenClaw update: DEFERRED, reminder set for tomorrow ⏳
- Liza Wolters email: PENDING action ⏳
