# 2026-02-10 — Claude Code Recovery + Gemini Skill

## Session Summary
Continued work on sub-agent infrastructure after yesterday's database import.

## Key Accomplishments

### Claude Code Setup Recovery
- **Found original setup conversation**: `claude-code-session.jsonl` contains the actual conversation where Zsolt set up Claude Code on Feb 6
- Shows Zsolt's initial guidance: "you are running on a VPS as my personal assistant with openclaw, i need to make sure you dont do stupid thing"
- Contains massive OpenClaw documentation dump that Claude Code generated
- **Next step**: Parse this JSONL to extract the original CLAUDE.md content and recreate configuration

### Gemini Transcription Skill
- Created `/home/node/.openclaw/workspace/skills/gemini/` with:
  - `SKILL.md` — Skill documentation
  - `transcribe.py` — Audio transcription using Files API
  - `youtube.py` — YouTube transcript extraction
- **Key insight**: Use `gemini-2.5-pro` instead of `2.0-flash` — different quota pool, avoids rate limits
- **Files API advantage**: Upload once, transcribe once — no chunking needed for audio up to 9.5 hours

### Knowledge Graph Lesson (IMPORTANT)
- **ALWAYS use `knowledge_graph` tool FIRST** before manual database searches
- Wasted 20 minutes searching manually when graph had Claude Code info immediately (node 318)
- Graph currently has: 85 nodes, 55 edges, 1,959 tags

## Claude Code Files (for reference)
Located in `/home/node/.openclaw/workspace/claude-code/`:
- `CONTEXT.md` — DB schema, scripts, workspace layout (THE MEMORY FILE)
- `INSTRUCTIONS.md` — Role and behavior guide
- `KNOWLEDGE-GRAPH-RESEARCH.md` — 939-line analysis of knowledge graph needs
- `claude-code-session.jsonl` — Original setup conversation

## Run Command
```bash
# PTY mode required!
claude "your task here"
```
Working directory: `/home/node/.openclaw/workspace/claude-code`

## Git Commits Today
- `0b3df7f` — Gemini skill + Claude Code instructions
- `63d1b7b` — Context references added

## Claude Code Setup RESOLVED

**What happened:**
- Zsolt shared **Claude Studio** documentation on Feb 9 (messages 17472, 17502)
- These docs describe his session persistence system with hooks, CLAUDE.md memory, and multi-agent orchestration
- I was searching the wrong timeframe (Feb 1-6) when the docs were sent Feb 9

**Key documents found:**
- `Claude Studio - Complete Implementation` — parallel agents, security hooks, logging
- `Portable Session Preservation System` — PostgreSQL session capture, context recovery, embeddings

**Created proper CLAUDE.md** based on this pattern:
- Role: Sub-agent under Nimbus
- Communication: via sessions_spawn, shared workspace
- Safety rules: from original Feb 1 setup + Claude Studio patterns
- Context files: CONTEXT.md, KNOWLEDGE-GRAPH-RESEARCH.md, INSTRUCTIONS.md
- Commit: `b43e6e9`

## Late Session: Claude Code Proposal FOUND ✅

Zsolt shared the full Feb 6th chat log containing the Claude Code setup conversation.

**Key proposal points (from Feb 6, ~01:30-03:00 UTC):**

### Division of Labor
| Nimbus | Claude Code |
|--------|-------------|
| Human interface (WhatsApp, email, voice) | Dev-focused CLI work |
| Persistent memory + knowledge graph | No persistent memory |
| Runs the tools (DB queries, scripts) | Advises, produces code |
| Messaging capabilities | No messaging |
| Proactive heartbeats | Task-based only |

**Core insight:** "You don't need Claude Code to do the same things you do. That's yours."

### Communication Methods
- `claude-msg "question"` — quick sync (claude-msg wrapper script)
- `run-task.sh "task-name" "prompt"` — async background task

### What Claude Code should know (its MEMORY.md):
1. Identity: Sub-agent under Nimbus, she executes, I advise
2. Rules: chown node:node, never touch openclaw.json/credentials/sessions, no gateway restarts
3. DB schema essentials (nodes, edges, content_tags, structured_messages, emails)
4. Key scripts (graph-query.py, graph-extract.py, db-watcher.py, check-email.js, send-email.js)
5. How Nimbus calls it

**Updated:** `claude-code/CLAUDE.md` with all proposal details

## Next Session TODO
1. **Ask Zsolt about Claude Code proposal** — when/where was it shared?
2. Test Claude Code with the new CLAUDE.md setup
3. Process knowledge graph extraction backlog (~2,000 messages pending)
4. Enable knowledge graph plugin in Nimbus agent config
5. Consider implementing Claude Studio hooks for session persistence
